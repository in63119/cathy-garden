import { HeroSection } from "@/components/hero-section";

export default function HomePage() {
  return (
    <div style={{ paddingBottom: "48px" }}>
      <HeroSection
        title="Cathy Garden"
        description="A private photo and video archive for uploading, organizing, revisiting, and sharing the moments worth keeping."
      />
    </div>
  );
}
